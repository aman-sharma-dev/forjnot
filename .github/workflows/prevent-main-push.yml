name: Prevent Direct Push

on:
  push:
    branches:
      - main
      - deployment

jobs:
  prevent-push:
    runs-on: ubuntu-latest
    steps:
      - name: Stop direct pushes
        run: |
          echo "🚫 Direct pushes to main or deployment are blocked."
          exit 1
